# integer

The field under validation must be an integer.


## Implementation

```js
export default {
  message: attribute => `The ${attribute} must be an integer.`
  // check: (value, { }) => {}
};

```

## Default message

```

```
