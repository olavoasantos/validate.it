import requiredWithoutAll from './index';

describe('requiredWithoutAll rule', () => {
  /** @test */
  it('should pass', () => {
    // expect(requiredWithoutAll.check( __INPUT__ )).toBeTruthy();
  });
});
